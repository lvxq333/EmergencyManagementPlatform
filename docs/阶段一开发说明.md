# 阶段一：基础档案、真实接入与数据集管理

本轮已完成阶段一数据闭环：登录 → 登记站点、设备和管段 → 配置设备独立密钥 → 设备持续 HTTP 上报或 CSV 导入 → 校验入库 → 最新值与历史查询。首页“数据接入”已接入 Java 后端；其他专题仍使用原有演示数据，不代表已经接通预测模型。

## 已实现

- 站点新增、编辑：编号、名称、类型、管理状态、区域、经纬度和坐标系。
- 设备新增、编辑、启停：所属站点、设备类型、接入协议、指标和预期上报周期。
- 管段新增、编辑及设备多管段关联：类型、区域、长度、数据来源和可选 GeoJSON 均可持久化。
- 指标字典当前内置 `vertical_displacement`（垂直位移，mm），包含合理范围和采样说明；接收解算后的位移，不接收或解算 GNSS 原始数据。
- 每台设备可生成独立接入密钥。密钥只显示一次，服务端仅保存 SHA-256 哈希；轮换后旧密钥立即失效。
- 真实设备通过 `POST /api/device-ingest/observations` 持续 HTTP 上报，使用 `X-Device-Code` 与 `X-Device-Key` 认证；同一消息 ID 可安全重试，每分钟每设备最多 600 次请求。
- 设备接入成功、重复及失败请求均记录状态、原因、来源地址和时间，页面可直接查看。
- CSV 文件先保存为数据集原始版本并逐行校验。校验通过前不写入正式观测表；管理员发布后原子入库，失败批次不会污染正式数据。
- CSV 错误可定位到行，覆盖未知设备、停用设备、重复消息、非法时间、指标、单位、数值和来源不一致。
- 数据集记录编号、来源、使用范围、原文件校验值、创建人、状态和发布时间，观测记录标明 HTTP 用户上报、真实设备上报、CSV 导入或模拟器来源。
- 登录用户仍可通过原 HTTP 接口提交单条数据，明确区分 SIMULATED / REAL / UNKNOWN；设备密钥接口中的 REAL 表示由已登记设备凭证提交，但仍需设备方保障传感器真实性。
- 可为启用设备配置服务器端自动模拟器，设置基准值、波动幅度和生成间隔；任务在离开页面后仍持续运行，并可随时停止。配置及进度保存在数据库，服务重启后可继续。
- 页面汇总累计记录、真实/模拟/未知来源数量及运行中的模拟器数量，设备行显示各来源数据构成。
- 同设备同消息 ID 同内容重试不重复入库；同 ID 不同内容返回冲突。迟到记录不会覆盖采集时间更新的最新值。
- 单位、时间、设备状态等校验；历史分页；超过三个预期上报周期无数据时显示超时提示。该提示不是独立心跳或预警算法。
- 新接口验证登录令牌、账户状态和权限；仅管理员维护档案、生成设备密钥和发布数据集，数据录入权限允许上传校验，风险查看权限允许读取。账户停用后无法登录。
- 档案、凭证、关联、模拟器、数据集导入和发布操作写入审计日志。

有观测记录后禁止修改设备编号、站点和指标，避免破坏历史归属。暂不提供删除功能。

## 本地运行

在项目根目录启动隔离数据库（需要 Docker）：

```sh
docker compose -p risk-platform-phase1 -f deploy/phase1.compose.yml up -d
```

数据库只映射本机 13316，使用独立命名卷，不复用其他项目数据库。首次创建数据卷自动加载基础表和阶段一表；已有卷不会自动重跑初始化脚本。

另一个终端进入 `backend-java`：

```sh
mvn spring-boot:run -Dspring-boot.run.profiles=phase1-local
```

在项目根目录另开终端：

```sh
node tools/phase1-preview.mjs
```

访问 <http://127.0.0.1:18080/login.html>。预览服务代理 `/api` 到本机 Java 服务 18081，仅供本地开发。当前测试数据库已创建账号 `phase1-admin`，密码 `Phase1-local-2026!`，并留下一个模拟站点、一台模拟设备和一条模拟观测用于检查。全新数据库不会自动创建此账号，需要注册后在隔离数据库授权管理员角色。

上述账号及配置中的数据库口令仅供本机开发，不得用于公网或正式环境。后端、预览服务可用各自终端 Ctrl+C 停止；数据库停止命令如下，保留数据卷：

```sh
docker compose -p risk-platform-phase1 -f deploy/phase1.compose.yml stop
```

## 现有部署升级

全新数据库执行 `backend-java/src/main/resources/schema.sql` 和 `backend-java/src/main/resources/db/phase1.sql`。已经执行过旧版阶段一脚本的环境，先备份数据库，再执行增量脚本 `backend-java/src/main/resources/db/phase1-completion.sql`。脚本只新增表和缺失字典记录，不删除旧数据，可重复执行。不要用基础 `schema.sql` 替换现有数据库。

重新构建并部署 Java 后端，同时发布 `home.html`、`assets/monitoring.js`、`assets/monitoring.css` 和修改后的 `assets/dashboard.js`。必须确保 `/api` 指向 Java 后端；原 Node 后端未实现本轮接口。不要把本地开发配置用于生产。

## 接口约定

所有接口携带 `Authorization: Bearer <登录接口返回的 token>`。

| 接口 | 用途 |
| --- | --- |
| GET /api/stations；POST /api/stations；PUT /api/stations/{id} | 站点列表、新建、编辑 |
| GET /api/devices；POST /api/devices；PUT /api/devices/{id} | 设备列表、新建、编辑 |
| PATCH /api/devices/{id}/status | 启停，请求体 `{"enabled":false}` |
| GET /api/metrics/definitions | 指标与标准单位 |
| POST /api/ingest/observations | 上报记录 |
| GET /api/devices/{id}/latest | 按采集时间获取最新值 |
| GET /api/devices/{id}/observations?limit=20&offset=0 | 历史分页，limit 最大 200 |
| PUT /api/devices/{id}/simulation；DELETE /api/devices/{id}/simulation | 启动/更新和停止自动模拟 |
| GET /api/monitoring/summary | 记录来源及运行任务汇总 |
| POST /api/devices/{id}/credentials | 管理员生成或轮换设备密钥，明文只返回一次 |
| POST /api/device-ingest/observations | 真实设备凭证持续上报 |
| GET /api/device-ingest/logs | 查看接入成功与失败日志 |
| GET/POST/PUT /api/pipeline-segments | 管段档案管理 |
| PUT /api/devices/{id}/pipeline-segments | 保存设备与管段关系 |
| GET /api/datasets/import-template | 下载 CSV 模板 |
| POST /api/datasets/import-jobs | 上传 CSV 并逐行校验 |
| GET /api/datasets/import-jobs/{id} | 查看导入结果和行级错误 |
| GET /api/datasets；POST /api/datasets/{id}/publish | 数据集列表与发布入库 |
| GET /api/audit-logs | 管理员查看阶段一操作审计 |

上报示例（设备需先登记，时间换成实际采集时间）：

```json
{
  "deviceCode": "DEMO-D001",
  "messageId": "unique-message-001",
  "eventTime": "2026-09-15T03:00:00Z",
  "metricCode": "vertical_displacement",
  "value": 1.62,
  "unit": "mm",
  "sourceType": "SIMULATED"
}
```

重试时保持原 messageId、采集时间和内容不变；新的采样使用新的 messageId。接口返回记录 id 和 duplicate 标志。时间采用带时区 ISO 8601；禁止超出服务器当前时间 60 秒以上的数据。不进行隐式单位换算。

## 验证与边界

`backend-java` 下运行 `mvn test`。当前共 21 项测试通过，集成测试覆盖档案、权限、停用账户、设备密钥、真实网络上报、错误凭证审计、幂等与并发重试、迟到数据、分页、管段关系、CSV 校验隔离、数据集发布和自动模拟任务。

自动模拟器属于流程验证数据源，不是传感器实时采集。设备端应在收到非 2xx 响应或网络中断时保留原消息 ID、采集时间和内容，恢复后重试；平台保证幂等接收，但设备本地缓存由设备程序实现。当前真实持续接入为 HTTP，MQTT 仅可登记为协议档案，实际 MQTT 消费、消息队列、300 站压测、预测任务和预警处置属于阶段二及以后。原有系统其他接口的整体安全加固尚未全部完成，不能据此宣称可直接公网生产运行。

## 下一步

进入阶段二：实现 MQTT 适配、原始消息持久化、可恢复处理任务、失败重试与积压监测；随后建设可复现的仿真回放和 300 基准站负载测试源。阶段二完成后再进入统一预测接口与调度。
